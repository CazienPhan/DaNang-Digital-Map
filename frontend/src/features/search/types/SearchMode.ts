/**
 * SearchMode — the two independent search workflows supported by this application.
 *
 * "place"   — Map4D Place Search (existing, fully implemented)
 * "product" — Meilisearch Product Search (business logic: future phases)
 */
export type SearchMode = "place" | "product";
